Name: Aziza K. Shukurova
Student ID: 2020147599

Part E:

First, the program checks if the inputted number of arguments coincides with the required format. If not, the program exits with an error message. If the first argument is read, depending on the mode, the program procedes to operation. If the mode is invalid, the program exits with an error message. 

For encryption, any file-related problems are treated as errors. If everything runs smoothly, the program encrypts a file and produces a tag. The same goes to decryption.